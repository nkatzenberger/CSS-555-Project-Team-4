Run this command in terminal to get BrainBrowser files on local machine: git clone https://github.com/aces/brainbrowser.git


NOW uses flask; Install flask and flask_cors, then run 
* TO START DEV SERVER
python main.py
*

access on machine in address bar of browser "http://localhost:8000/"

DEPENDENCIES
pip install firebase_admin
pip install pytest
pip install pandas
pip install flask
pip install flask_cors
pip install scipy
pip install numpy
pip install h5py
pip install scikit-learn
pip install torch
pip install torchvision
pip install mne
pip install argparse
pip install matplotlib